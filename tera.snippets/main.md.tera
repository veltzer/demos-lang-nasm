## Number of examples

Currently there are {{ git_count_files(pattern="src/examples/*.asm") }} examples in this repo.

Currently there are {{ git_count_files(pattern="src/exercises/*.asm") }} exercises in this repo.

## How to run examples from this repository?

```console
$ make
$ ./src/hello_world.elf
```
